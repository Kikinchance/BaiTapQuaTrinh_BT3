<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.BaiTapQuaTrinh_BT3">
        <activity
            android:name=".MusicControlActivity"
            android:exported="false" />
        <activity
            android:name=".VolumeControlActivity"
            android:exported="false" />
        <activity
            android:name=".IoTControlActivity"
            android:exported="false" />
        <activity
            android:name=".TvRemoteActivity"
            android:exported="false" />
        <activity
            android:name=".AirConditionerActivity"
            android:exported="false" />

        <activity android:name=".FlashControlActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>